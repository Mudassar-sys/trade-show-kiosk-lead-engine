{
  "name": "trade-show-labs-kiosk",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first claw-machine lead-capture kiosk MVP. Persist-then-fire durability core, mock + serial hardware layer, idempotent cloud sync.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
